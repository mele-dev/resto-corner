# 🗺️ Diagrama del Proceso de Deploy

## Flujo Completo: De Código a Producción

```
┌─────────────────────────────────────────────────────────────────┐
│                    TU CÓDIGO LOCAL                              │
│  CornerApp/backend-csharp/CornerApp.API/                       │
│    ├── Program.cs                                              │
│    ├── Dockerfile                                              │
│    ├── Controllers/                                            │
│    └── ...                                                     │
└───────────────────────┬─────────────────────────────────────────┘
                        │
                        │  git push origin main
                        ▼
┌─────────────────────────────────────────────────────────────────┐
│                    GITHUB REPOSITORY                            │
│  https://github.com/tu-usuario/Restaurante                     │
│    ├── CornerApp/backend-csharp/                              │
│    ├── QUICK_START.md                                          │
│    └── scripts/                                                │
└───────────────────────┬─────────────────────────────────────────┘
                        │
                        │  Railway detecta push
                        │  (auto-deploy habilitado)
                        ▼
┌─────────────────────────────────────────────────────────────────┐
│                    RAILWAY.APP                                  │
│                                                                 │
│  ┌──────────────────────────────────────────────────┐         │
│  │  Build Process                                   │         │
│  │  1. Clone repo                                   │         │
│  │  2. cd CornerApp/backend-csharp                 │         │
│  │  3. docker build -f CornerApp.API/Dockerfile    │         │
│  │  4. docker push to Railway registry             │         │
│  └──────────────────────────────────────────────────┘         │
│                        │                                        │
│                        ▼                                        │
│  ┌──────────────────────────────────────────────────┐         │
│  │  Deploy Process                                  │         │
│  │  1. Pull Docker image                            │         │
│  │  2. Inject environment variables                 │         │
│  │     - CONNECTION_STRING                          │         │
│  │     - JWT_SECRET_KEY                            │         │
│  │     - ASPNETCORE_URLS=http://+:8080             │         │
│  │  3. Start container on port 8080                │         │
│  │  4. Health check: GET /health                   │         │
│  └──────────────────────────────────────────────────┘         │
│                        │                                        │
│                        ▼                                        │
│  ┌──────────────────────────────────────────────────┐         │
│  │  MySQL Database (Internal)                       │         │
│  │  mysql.railway.internal:3306                     │         │
│  │    └── railway database                          │         │
│  └──────────────────────────────────────────────────┘         │
│                        │                                        │
│                        ▼                                        │
│  ┌──────────────────────────────────────────────────┐         │
│  │  Public URL Generated                            │         │
│  │  https://cornerapp-production.up.railway.app     │         │
│  │    ├── /health       (Health check)             │         │
│  │    ├── /swagger      (API docs)                 │         │
│  │    ├── /api/*        (Public endpoints)         │         │
│  │    └── /admin/api/*  (Admin endpoints)          │         │
│  └──────────────────────────────────────────────────┘         │
└─────────────────────────────────────────────────────────────────┘
                        │
                        │  HTTPS (automático)
                        ▼
┌─────────────────────────────────────────────────────────────────┐
│                    INTERNET                                     │
│  Usuarios acceden a tu API desde:                              │
│    - Frontend web                                              │
│    - Aplicación móvil                                          │
│    - Postman / Insomnia                                        │
│    - Otros servicios                                           │
└─────────────────────────────────────────────────────────────────┘
```

---

## 📊 Arquitectura Simplificada

```
┌─────────────┐      HTTPS/443       ┌──────────────────┐
│   CLIENTE   │ ───────────────────► │  Railway Proxy   │
│  (Browser,  │                       │  (Load Balancer) │
│   Mobile)   │ ◄─────────────────── │   + SSL/TLS      │
└─────────────┘                       └──────────────────┘
                                              │
                                              │ HTTP/8080
                                              │
                                      ┌───────▼───────────┐
                                      │  Docker Container │
                                      │  CornerApp.API    │
                                      │  (ASP.NET Core)   │
                                      └───────┬───────────┘
                                              │
                                              │ MySQL protocol
                                              │
                                      ┌───────▼───────────┐
                                      │  MySQL Database   │
                                      │  (Railway)        │
                                      └───────────────────┘
```

---

## 🔄 Ciclo de Vida del Deploy

### 1. Desarrollo Local
```
┌──────────────┐
│  Developer   │
│  escribe     │
│  código      │
└──────┬───────┘
       │
       │ git commit
       │ git push origin main
       ▼
```

### 2. Build Automático
```
┌──────────────────────────────────┐
│  Railway detecta push a main     │
│  ├─ 1. Clone repo                │
│  ├─ 2. Read Dockerfile           │
│  ├─ 3. Build image                │
│  │     (multi-stage build)       │
│  └─ 4. Push to registry          │
└──────────────┬───────────────────┘
               │
               │ Build exitoso
               ▼
```

### 3. Deploy Automático
```
┌──────────────────────────────────┐
│  Railway despliega nuevo build   │
│  ├─ 1. Pull image                │
│  ├─ 2. Stop old container        │
│  ├─ 3. Start new container       │
│  ├─ 4. Health check (30s)        │
│  │     GET /health               │
│  ├─ 5. Route traffic             │
│  └─ 6. Remove old container      │
└──────────────┬───────────────────┘
               │
               │ Deploy completo
               ▼
```

### 4. Producción
```
┌──────────────────────────────────┐
│  API en producción               │
│  ├─ URL pública accesible        │
│  ├─ HTTPS habilitado             │
│  ├─ MySQL conectado              │
│  ├─ Logs en tiempo real          │
│  └─ Métricas disponibles         │
└──────────────────────────────────┘
```

---

## 🔐 Flujo de Autenticación

```
┌──────────┐                    ┌─────────────────┐                  ┌──────────┐
│  CLIENT  │                    │  CornerApp API  │                  │  MySQL   │
└────┬─────┘                    └────────┬────────┘                  └────┬─────┘
     │                                   │                                │
     │ POST /admin/api/auth/login        │                                │
     │ { username, password }            │                                │
     │──────────────────────────────────►│                                │
     │                                   │                                │
     │                                   │ SELECT * FROM Admins           │
     │                                   │ WHERE Username = ?             │
     │                                   │───────────────────────────────►│
     │                                   │                                │
     │                                   │◄───────────────────────────────│
     │                                   │ User data + hashed password    │
     │                                   │                                │
     │                                   │ Verify password with BCrypt    │
     │                                   │ Generate JWT token             │
     │                                   │ (signed with JWT_SECRET_KEY)   │
     │                                   │                                │
     │◄──────────────────────────────────│                                │
     │ { token: "eyJhbG...", ... }       │                                │
     │                                   │                                │
     │ GET /admin/api/orders             │                                │
     │ Authorization: Bearer eyJhbG...   │                                │
     │──────────────────────────────────►│                                │
     │                                   │                                │
     │                                   │ Validate JWT token             │
     │                                   │ Extract user claims            │
     │                                   │                                │
     │                                   │ SELECT * FROM Orders           │
     │                                   │───────────────────────────────►│
     │                                   │                                │
     │◄──────────────────────────────────│◄───────────────────────────────│
     │ { orders: [...] }                 │                                │
     │                                   │                                │
```

---

## 🗄️ Base de Datos - Inicialización

```
┌──────────────────────────────────────────────────────┐
│  Container Start                                     │
└────────────────────┬─────────────────────────────────┘
                     │
                     ▼
┌──────────────────────────────────────────────────────┐
│  ApplicationDbContext.EnsureCreatedAsync()           │
│  ├─ Verifica si la DB existe                        │
│  ├─ Si no existe, crea todas las tablas:            │
│  │   ├─ Restaurants                                 │
│  │   ├─ Admins                                      │
│  │   ├─ Categories                                  │
│  │   ├─ Products                                    │
│  │   ├─ SubProducts                                 │
│  │   ├─ Orders                                      │
│  │   ├─ OrderItems                                  │
│  │   ├─ DeliveryPersons                             │
│  │   ├─ Tables                                      │
│  │   └─ ... (más tablas)                            │
│  └─ Si ya existe, verifica esquema                  │
└────────────────────┬─────────────────────────────────┘
                     │
                     ▼
┌──────────────────────────────────────────────────────┐
│  Datos de Demostración (solo en Production)         │
│  ├─ Restaurant (ID 12: "Corner Restaurant")         │
│  ├─ Admin (corner / password123)                    │
│  ├─ DeliveryPerson (juan_delivery / delivery123)    │
│  ├─ Category ("Bebidas")                            │
│  ├─ Products (Coca Cola, Pizza Margarita)           │
│  ├─ SubProducts (Tamaños)                           │
│  └─ Tables (Mesa 1, 2, 3)                           │
└────────────────────┬─────────────────────────────────┘
                     │
                     ▼
┌──────────────────────────────────────────────────────┐
│  API Lista para Recibir Requests                    │
└──────────────────────────────────────────────────────┘
```

---

## 📈 Monitoreo y Logs

```
┌─────────────────┐
│  Railway        │
│  Dashboard      │
└────┬────────────┘
     │
     ├─► Logs en tiempo real
     │   ├─ Application logs (Serilog)
     │   ├─ HTTP requests
     │   ├─ Errores y excepciones
     │   └─ Database queries (en dev)
     │
     ├─► Métricas
     │   ├─ CPU usage
     │   ├─ Memory usage
     │   ├─ Request rate
     │   ├─ Response time
     │   └─ Error rate
     │
     ├─► Health Checks
     │   ├─ /health         (overall)
     │   ├─ /health/ready   (readiness)
     │   ├─ /health/live    (liveness)
     │   └─ /health/detailed (full info)
     │
     └─► Deployment History
         ├─ Build logs
         ├─ Deploy time
         ├─ Commit SHA
         └─ Rollback option
```

---

## 🎯 Próximos Pasos

Una vez deployado:

1. **Configurar CORS** para tu frontend
2. **Agregar dominio custom** (opcional)
3. **Configurar Redis** para cache distribuido (opcional)
4. **Agregar monitoring** (Sentry, Application Insights, etc.)
5. **Configurar backups** automáticos de DB
6. **Agregar CI/CD tests** automáticos

---

**¿Listo para deployar?** → Lee [QUICK_START.md](QUICK_START.md)
